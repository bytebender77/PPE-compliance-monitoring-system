# ── Python ────────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/

# Virtual environments
.venv/
venv/
env/

# ── Dataset (large files — store in Roboflow or shared drive) ─────────────────
data/raw/videos/
data/raw/frames/
data/processed/images/
data/processed/labels/

# Keep directory structure and docs — exclude only the actual files
!data/raw/videos/.gitkeep
!data/raw/frames/.gitkeep
!data/processed/images/train/.gitkeep
!data/processed/images/val/.gitkeep
!data/processed/images/test/.gitkeep
!data/processed/labels/train/.gitkeep
!data/processed/labels/val/.gitkeep
!data/processed/labels/test/.gitkeep
!data/samples/.gitkeep

# ── Model weights (large binary files) ───────────────────────────────────────
*.pt
*.engine
*.onnx
*.tflite
models/
weights/
runs/

# Keep the models/ directory itself
!models/.gitkeep

# ── YOLOv8 training output ────────────────────────────────────────────────────
runs/

# ── Runtime output ────────────────────────────────────────────────────────────
screenshots/
logs/

# ── Large media / raw datasets ────────────────────────────────────────────────
*.mp4
*.avi
*.mov
*.MOV
*.mkv
cctv data/
vest.v1i.yolov8/
vest3.v2i.yolov8/
debug_snapshot.jpg

# ── Environment ───────────────────────────────────────────────────────────────
.env
.env.local
.env.*.local

# ── IDE ───────────────────────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# ── Reorganized folders (2026-06-16 cleanup) — large data/media/checkpoints ──
datasets/
media/
output/
# models/archive/ is already covered by the models/ rule above

# ── Roboflow dataset exports, merged datasets & archives (large, regenerable) ──
*.zip
*.yolov8*/
data/merged_*/
data/full_merged/
data/auto_annotated/
data/gloves_raw/
data/goggles_finetune/
data/ppe_capture/
data/vest_to_annotate/
data/zoomed_crops/
data/zoomed_crops_annotated/
data/raw/
final_data/
pptx_unpacked/
