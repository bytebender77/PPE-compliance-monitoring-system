# ══════════════════════════════════════════════════════════════════════════════
# PPE Compliance Monitoring System — Python dependencies
# Stage 1: Person Detection MVP
# ══════════════════════════════════════════════════════════════════════════════
#
# Install:
#   pip install -r requirements.txt
#
# GPU note:
#   PyTorch with CUDA is NOT listed here because the CUDA version depends on
#   your driver. Install separately from pytorch.org BEFORE running pip install:
#
#   # CUDA 11.8 example:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
#
#   # CPU only (works, just slower):
#   pip install torch torchvision
#
# Version pins:
#   Pinned to minor versions (==X.Y.*) not patches so security patches auto-apply
#   but breaking changes don't sneak in. Tighten to ==X.Y.Z in Docker.

# ── Core inference ─────────────────────────────────────────────────────────────
ultralytics>=8.0,<9.0          # YOLOv8 — detection, tracking export
opencv-python>=4.9,<5.0        # video capture, drawing, display

# ── Numerical / array ─────────────────────────────────────────────────────────
numpy>=1.24,<2.0               # array ops (pinned below 2.0 for compat)

# ── Notifications (Stage 4) ───────────────────────────────────────────────────
requests>=2.31,<3.0            # WhatsApp Cloud API HTTP calls
python-dotenv>=1.0,<2.0        # load .env file for API keys

# ── Testing ────────────────────────────────────────────────────────────────────
pytest>=8.0,<9.0               # test runner
pytest-cov>=5.0,<6.0           # coverage reporting

# ══════════════════════════════════════════════════════════════════════════════
# FUTURE STAGES — commented out now, uncomment as each stage is implemented
# ══════════════════════════════════════════════════════════════════════════════

# Stage 2 — ByteTrack multi-object tracking
# supervision>=0.20,<1.0       # roboflow supervision (includes ByteTrack)

# Stage 4 — Compliance engine
# (no new deps — pure Python logic on top of existing detection output)

# Stage 5 — Alert & logging
# (SQLite is stdlib — no extra deps needed for our embedded logging)

# Stage 6 — Backend API
fastapi>=0.110,<1.0            # REST + WebSocket API
uvicorn[standard]>=0.29,<1.0  # ASGI server for FastAPI
pydantic>=2.0,<3.0             # data validation (FastAPI uses this internally)
python-multipart>=0.0.9        # needed for FastAPI file uploads

# Stage 7 — Production / deployment
pyyaml>=6.0,<7.0               # cameras.yaml parsing for multi-camera mode
structlog>=24.0,<25.0          # structured JSON logging for production
